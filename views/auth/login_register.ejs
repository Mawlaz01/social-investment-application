<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <link rel="stylesheet" href="/stylesheets/login_register.css">
    <title>SIAPP</title>
    <link rel="icon" type="image/x-icon" href="/images/user/Siapp.ico" />
</head>
<body>
    <% if (messages.error) { %>
        <div class="alert alert-danger">
            <i class="fas fa-times-circle"></i>
            <% messages.error.forEach(function(message) { %>
                <p><%= message %></p>
            <% }) %>
        </div>
    <% } %>
    <% if (messages.success) { %>
        <div class="alert alert-success">
            <i class="fas fa-check-circle"></i>
            <% messages.success.forEach(function(message) { %>
                <p><%= message %></p>
            <% }) %>
        </div>
    <% } %>
    <div class="container" id="container">
        <div class="form-container log-in">
            <form action="/login" method="POST">
                <h1>Log In</h1>
                <input type="email" name="email" class="input-signin" placeholder="Email" required>
                <input type="password" name="password" class="input-signin" placeholder="Password" required>
                <button type="submit">Log In</button>  
                <p class="message">Belum punya akun? <a class="hidden" id="register"><b>Sign Up</b></a></p>
            </form>
        </div>
        
        <div class="form-container sign-up" id="register-panel">
            <form action="/register" method="POST" enctype="multipart/form-data">
                <h1>Sign Up</h1>
                <label class="foto-label" for="foto">Unggah Foto</label>
                <input type="file" id="foto" name="foto" accept="image/*" style="display: none;">
                <input type="text" name="nama" class="input-signup" placeholder="Nama" required>
                <input type="text" name="NIK" class="nik" placeholder="NIK" required>
                <input type="text" name="no_wa" class="no-wa" placeholder="No WhatsApp" required>
                <input type="email" name="email" class="input-signup" placeholder="Email" required>
                <input type="password" name="password" class="input-signup" placeholder="Password" required>
                <button type="submit">Sign Up</button>
                <p class="message">Sudah punya akun? <a class="hidden" id="login"><b>Login</b></a></p>
            </form>
        </div>

        <div class="toggle-container">
            <div class="toggle">
                <div class="toggle-panel toggle-left">
                    <img src="/images/user/logo_siap_putih.png" alt="Logo" class="logo">
                    <h1>Welcome back to SIAPP!</h1>
                    <p>Senang melihat Anda kembali! Mari lanjutkan pengelolaan sumbangan Anda dengan mudah dan terorganisir.</p>
                </div>
                
                <div class="toggle-panel toggle-right">
                    <img src="/images/user/logo_siap_putih.png" alt="Logo" class="logo">
                    <h1>Welcome to SIAPP!</h1>
                    <p>Bergabunglah bersama kami untuk mempermudah pengelolaan sumbangan dalam acara-acara istimewa Anda.</p>
                </div>
            </div>
        </div>
    </div>

    <script src="/js/login_register.js"></script>
</body>
</html>
